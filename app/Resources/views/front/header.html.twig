{% set curr_route = app.request.attributes.get('_route') %}
<header>
    <div class="wrapper">
        <nav>
            <ul>
                <li>
                    <a href="{{ path('homepage') }}" {% if curr_route == 'homepage' %}class="active regular"{% endif %}>À propos</a>
                </li>
                <li>
                    <a href="{{ path('projects') }}" {% if curr_route == 'projects' %}class="active regular"{% endif %}>Projets</a>
                </li>
            </ul>
        </nav>
        <img src="{% image 'images/circle_picture.png' %}{{ asset_url }}{% endimage %}" class="logo" alt="Logo">
        <h1 class="bold">{{ content.name|raw }}</h1>
        <h2 class="regular">{{ content.job_title|raw }}, {{ content.city|raw }}</h2>
    </div>
</header>